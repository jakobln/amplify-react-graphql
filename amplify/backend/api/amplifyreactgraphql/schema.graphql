type Home @model @auth(rules: [{allow: public}]) {
  id: ID!
  address: String
  price: Float
  image_url: String
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
} 